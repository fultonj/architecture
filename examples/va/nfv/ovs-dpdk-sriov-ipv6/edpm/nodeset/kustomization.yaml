---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reuse the IPv4 nodeset configuration entirely - it's identical for IPv6
# This inherits both the VA component and the values.yaml
resources:
  - ../../../ovs-dpdk-sriov/edpm/nodeset
